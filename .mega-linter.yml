# MegaLinter configuration
# https://megalinter.io/latest/configuration/

# Linters whose errors should not fail the build
# (pre-existing issues that are out-of-scope for this PR)
DISABLE_ERRORS_LINTERS:
  - COPYPASTE_JSCPD
  - REPOSITORY_GRYPE
